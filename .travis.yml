os:
    - linux
    - osx

install:
    - cd ${TRAVIS_BUILD_DIR}
    - ./travis-get-deps.sh
language: cpp
compiler:
    - gcc
    - clang

script:
    - cd ${TRAVIS_BUILD_DIR}
    - make all test-rss
    - cd test && ./test-rss
